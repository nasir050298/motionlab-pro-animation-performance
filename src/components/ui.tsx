import{ReactNode}from"react";import{motion}from"framer-motion";import{fadeUp,transition}from"../lib/motion";export function SectionTitle({eyebrow,title,text}:{eyebrow?:string;title:string;text?:string}){return <motion.div variants={fadeUp} initial="hidden" whileInView="visible" viewport={{once:true,amount:.3}} transition={transition} className="mx-auto mb-12 max-w-3xl text-center">{eyebrow&&<span className="lab-badge">{eyebrow}</span>}<h2 className="mt-4 text-4xl font-black tracking-tight md:text-6xl">{title}</h2>{text&&<p className="mt-4 text-lg leading-8 text-slate-300">{text}</p>}</motion.div>}export function Panel({children,className=""}:{children:ReactNode;className?:string}){return <div className={`lab-panel ${className}`}>{children}</div>}export function MetricCard({label,value,note}:{label:string;value:string;note:string}){return <motion.div whileHover={{y:-6,scale:1.02}} className="lab-card p-5"><p className="text-xs font-black uppercase tracking-[0.15em] text-slate-400">{label}</p><p className="mt-3 text-4xl font-black text-lab-cyan">{value}</p><p className="mt-1 text-sm text-slate-400">{note}</p></motion.div>}export function Badge({children}:{children:ReactNode}){return <span className="lab-badge">{children}</span>}
