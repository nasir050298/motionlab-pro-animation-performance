import{motion,useReducedMotion}from"framer-motion";import{ReactNode}from"react";import{fadeUp,transition}from"../../lib/motion";export function Reveal({children,delay=0,className=""}:{children:ReactNode;delay?:number;className?:string}){const reduce=useReducedMotion();return <motion.div variants={reduce?undefined:fadeUp} initial={reduce?false:"hidden"} whileInView={reduce?undefined:"visible"} viewport={{once:true,amount:.25}} transition={{...transition,delay}} className={className}>{children}</motion.div>}
