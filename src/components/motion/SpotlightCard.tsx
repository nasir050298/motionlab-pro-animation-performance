import{ReactNode,useState}from"react";export function SpotlightCard({children}:{children:ReactNode}){const[pos,setPos]=useState({x:50,y:50});return <div onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();setPos({x:((e.clientX-r.left)/r.width)*100,y:((e.clientY-r.top)/r.height)*100})}} className="relative overflow-hidden rounded-[1.7rem] border border-white/10 bg-white/[0.06] p-6"><div className="pointer-events-none absolute inset-0 opacity-80" style={{background:`radial-gradient(circle at ${pos.x}% ${pos.y}%, rgba(0,229,255,.20), transparent 35%)`}}/><div className="relative">{children}</div></div>}
