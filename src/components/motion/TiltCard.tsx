import{motion,useMotionValue,useSpring,useTransform}from"framer-motion";import{ReactNode}from"react";export function TiltCard({children}:{children:ReactNode}){const x=useMotionValue(0),y=useMotionValue(0);const rotateX=useSpring(useTransform(y,[-.5,.5],[10,-10]),{stiffness:220,damping:22});const rotateY=useSpring(useTransform(x,[-.5,.5],[-10,10]),{stiffness:220,damping:22});return <motion.div style={{rotateX,rotateY,transformStyle:"preserve-3d"}} onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();x.set((e.clientX-r.left)/r.width-.5);y.set((e.clientY-r.top)/r.height-.5)}} onMouseLeave={()=>{x.set(0);y.set(0)}} className="lab-card p-6">{children}</motion.div>}
